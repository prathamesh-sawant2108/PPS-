#include <stdio.h>
int main() 
{
    int num;
    printf("Enter the number:");
    scanf("%d", &num);
    if(num>3)
    {
        printf("Yes\n");
    }
    printf("End the program");
    return 0;
}
